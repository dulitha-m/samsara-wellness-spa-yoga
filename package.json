{
  "name": "samsara-wellness-spa",
  "version": "1.0.0",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "install-all": "cd client && npm install --legacy-peer-deps",
    "dev": "npm run dev -w client",
    "build": "npm run build -w client",
    "preview": "npm run preview -w client"
  }
}

